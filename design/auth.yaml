app:
  name: "{{AppName}}"

  auth:

    password:
      enabled: true

    apikey:
      enabled: true

    oauth:
      google:
        enabled: false
      facebook:
        enabled: false
      linkedin:
        enabled: false
      github:
        enabled: false

