app:
  name: "{{AppName}}"

  auth:

    password:
      enabled: true

    apikey:
      enabled: true

    oauth:
      google: false
      facebook: false
      linkedin: false
      github: false

